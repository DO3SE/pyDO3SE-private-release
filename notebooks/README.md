This directory contains notebooks for experimenting with new features before implementing
them into the model.

# Notes

Make sure to enable widgets jupyter extension with
`jupyter nbextension enable --py widgetsnbextension1
