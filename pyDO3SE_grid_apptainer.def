BootStrap: docker
From: python:3.12-slim-bookworm

%post
    python -m pip install ./dist/pydo3se_open-4.43.1-py3-none-any.whl
    python -m pip install xarray dask netcdf4
    python -m pip install -U ./built_dependencies/data_helpers-0.2.1-py3-none-any.whl
    apt-get update && \
    apt-get install -y procps

%files
  $PWD/dist dist
  $PWD/built_dependencies built_dependencies


%runscript
  python -m pyDO3SE demo run

%labels
   Author Sam Bland