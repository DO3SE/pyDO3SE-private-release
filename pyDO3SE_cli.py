#!/usr/bin/env python3
from pyDO3SE.tools.cli import cli


if __name__ == "__main__":
    ''' Get the config and input data locations from the cli
    Then loads these into the config and runs the model

    '''
    cli()
